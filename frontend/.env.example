# Base URL of the SkillForge AI FastAPI backend.
VITE_API_URL=http://localhost:8000

# Optional: acts as a specific dev user against the backend's auth-dev-mode
# stub (see backend/app/dependencies.py). Leave unset to use the backend's
# default dev user.
VITE_DEV_USER_EMAIL=
